// Configuracoes de PRODUCAO - aponta pro backend no Render
// Essa URL vai ser atualizada quando criarmos o servico no Render
export const environment = {
  production: true,
  // IMPORTANTE: trocar pela URL real gerada pelo Render depois do deploy
  apiUrl: 'https://SEU-BACKEND.onrender.com'
};
